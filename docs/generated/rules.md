<!-- @generated by `cargo xtask codegen` from docs/spec/. DO NOT EDIT: CI fails on any diff. -->

# Diavolo format rules (82 total)

Every `file` and `chain` rule has broken fixtures in `corpus/broken/`, each violating exactly that rule.

## Phase `envelope`

Size, header magic, version refusal, header flags.

| id | level | rule | fixtures |
|---|---|---|---|
| `file.too_short` | MUST | Reject a file shorter than HEADER_LEN + BLOCK_HEADER_LEN + INDEX_HEADER_LEN + TRAILER_LEN. | `file-too-short` |
| `header.magic` | MUST | Reject unless header.magic == HEADER_MAGIC. | `header-magic` |
| `header.version_unsupported` | MUST | Reject unless (header.format_major, header.format_minor) is an accepted row of the acceptance table. | `header-version-major`, `header-version-minor` |
| `header.flags_unknown_odd` | MUST | Reject if header.header_flags has an unknown bit set at an odd position. Unknown even bits are ignored. | `header-flags-unknown-odd` |

## Phase `trailer`

Trailer magic, self digest, agreement with header, flags, header digest, reserved fields, classification, index geometry.

| id | level | rule | fixtures |
|---|---|---|---|
| `trailer.magic` | MUST | Reject unless the last TRAILER_LEN bytes begin with TRAILER_MAGIC. | `trailer-magic` |
| `trailer.self_digest` | MUST | Reject unless trailer.trailer_digest == BLAKE3(trailer bytes [0, 128)). | `trailer-self-digest` |
| `trailer.version_mismatch` | MUST | Reject unless trailer.format_major == header.format_major and trailer.format_minor == header.format_minor. | `trailer-version-mismatch` |
| `trailer.uuid_mismatch` | MUST | Reject unless trailer.archive_uuid == header.archive_uuid. | `trailer-uuid-mismatch` |
| `trailer.flags_unknown_odd` | MUST | Reject if trailer.trailer_flags has an unknown bit set at an odd position. | `trailer-flags-unknown-odd` |
| `trailer.header_digest` | MUST | Reject unless trailer.header_digest == BLAKE3(header bytes [0, HEADER_LEN)). | `trailer-header-digest` |
| `trailer.prev_offset_nonzero` | MUST | Reject unless trailer.prev_trailer_offset == 0. | `trailer-prev-offset` |
| `trailer.index_geometry` | MUST | Reject unless index_offset >= HEADER_LEN and index_offset + BLOCK_HEADER_LEN + index_len + TRAILER_LEN == archive_len, computed without overflow. | `trailer-index-geometry` |
| `file.size_unclassifiable` | MUST | The file is an archive if its size == archive_len, else a sidecar if its size == HEADER_LEN + BLOCK_HEADER_LEN + index_len + TRAILER_LEN (INDX block at HEADER_LEN); reject if neither. When both hold it is an archive. | `file-size-mismatch` |

## Phase `index_seal`

INDX block header, its agreement with the trailer, and BLAKE3 of the payload. Nothing in the payload is parsed before this passes.

| id | level | rule | fixtures |
|---|---|---|---|
| `block.header_check` | MUST | Reject a block whose header_check != first 8 bytes of BLAKE3(block header bytes [0, 48)). | `index-block-header-check`, `data-block-header-check` |
| `index.block_type` | MUST | Reject unless the block at the index position has type_id INDX. The index position is trailer.index_offset for an archive and HEADER_LEN for a sidecar, as classified by file.size_unclassifiable; every index_seal rule reads the block at that position. | `index-block-type` |
| `block.flags_unknown_odd` | MUST | Reject a block whose flags have an unknown bit set at an odd position. | `index-block-flags-unknown-odd` |
| `block.skip_on_known` | MUST | Reject a DATA or INDX block with flags.skip set. | `index-block-skip`, `data-block-skip` |
| `index.len_mismatch` | MUST | Reject unless the payload_len of the block at the index position == trailer.index_len. | `index-len-mismatch` |
| `index.block_digest_mismatch` | MUST | Reject unless the payload_digest of the block at the index position == trailer.index_digest. | `index-block-digest-mismatch` |
| `index.digest` | MUST | Reject unless BLAKE3 of the payload of the block at the index position == trailer.index_digest. No payload byte is interpreted before this passes. | `index-digest` |

## Phase `index_structure`

Stage 1: index header enums/flags/reserved/ranges, table geometry, heap bounds, every record's enums/flags/reserved/ranges.

| id | level | rule | fixtures |
|---|---|---|---|
| `index.too_short` | MUST | Reject an INDX payload shorter than INDEX_HEADER_LEN. | `index-too-short` |
| `index.codec` | MUST | Reject unless index_header.index_codec is a defined non-invalid index_codec value. | `index-codec-invalid`, `index-codec-unknown` |
| `index.source_fs` | MUST | Reject unless index_header.source_fs is a defined non-invalid source_fs value. | `index-source-fs` |
| `index.capabilities_unknown_odd` | MUST | Reject if index_header.capabilities has an unknown bit set at an odd position. | `index-capabilities-unknown-odd` |
| `record.reserved_nonzero` | MUST | Reject if any field marked reserved in any layout is nonzero. | `index-reserved-nonzero`, `inode-reserved-nonzero`, `data-reserved-nonzero` |
| `time.nsec_range` | MUST | Reject if any *_nsec field is >= NSEC_LIMIT. | `index-capture-nsec`, `inode-mtime-nsec` |
| `index.geometry` | MUST | Reject unless INDX payload_len == INDEX_HEADER_LEN + sum(count * record size) + heap_len over the defined tables, computed without overflow. | `index-geometry` |
| `index.tombstones_nonzero` | MUST | Reject unless index_header.tombstone_count == 0, and every dir_record's tombstone_first and tombstone_count == 0. | `index-tombstone-count`, `dir-tombstone-count` |
| `heap.bounds` | MUST | Reject if any heap reference has offset + len > heap_len, computed without overflow. | `heap-bounds-dirent`, `heap-bounds-symlink` |
| `heap.empty_offset` | MUST | Reject a zero-length heap reference whose offset is not 0. | `heap-empty-offset` |
| `index.writer_empty` | MUST | Reject if the writer heap reference is empty. | `index-writer-empty` |
| `inode.present_unknown_odd` | MUST | Reject if an inode's present flags have an unknown bit set at an odd position. | `inode-present-unknown-odd` |
| `inode.content_kind` | MUST | Reject unless every inode's content_kind is a defined non-invalid value. | `inode-content-kind-invalid`, `inode-content-kind-unknown` |
| `inode.file_type` | MUST | Reject unless every inode's mode & 0xF000 is a defined non-invalid file_type. | `inode-file-type` |
| `dir.completeness` | MUST | Reject unless every dir_record's completeness is complete. invalid, partial (registered, undefined) and unlisted values are all rejected. | `dir-completeness-invalid`, `dir-completeness-partial` |
| `extent.raw_len_range` | MUST | Reject an extent whose raw_len is 0 or > MAX_CHUNK_RAW. | `extent-raw-len-zero`, `extent-raw-len-large` |
| `extent.offset_in_block_nonzero` | MUST | Reject an extent whose offset_in_block != 0. | `extent-offset-in-block` |

## Phase `index_semantics`

Stage 2, only on stage-1-valid data: order, tiling, per-type rules, namespace.

| id | level | rule | fixtures |
|---|---|---|---|
| `index.base_ref` | MUST | Reject unless base_ref <= chain_count, and base_ref == 0 implies chain_count == 0. | `index-base-ref-range`, `index-base-ref-zero-with-chain` |
| `chain.self_or_duplicate` | MUST | Reject a chain entry whose archive_uuid equals this archive's, or equals another entry's. | `chain-self`, `chain-duplicate` |
| `inode.order` | MUST | Reject unless every inode's ino != 0 and inodes are strictly ascending by ino. | `inode-order`, `inode-ino-zero` |
| `inode.content_kind_for_type` | MUST | Reject an inode whose content_kind is not admitted for its file_type by content_kind_by_type. | `inode-kind-for-type` |
| `inode.size_for_type` | MUST | Reject unless size == 0 for every type other than reg and lnk, and 1 <= size <= SYMLINK_MAX for lnk. | `inode-dir-size`, `inode-symlink-size` |
| `inode.fields_for_kind` | MUST | Reject unless: extent_count == 0 unless extents; chain_ref != 0 iff inherited; target_off == 0 unless symlink; rdev_major == rdev_minor == 0 unless chr or blk; content_digest all-zero unless extents or inherited. | `inode-extents-on-none`, `inode-chain-ref-missing`, `inode-rdev-on-reg`, `inode-digest-on-dir` |
| `inode.chain_ref_range` | MUST | Reject an inherited inode whose chain_ref > chain_count. | `inode-chain-ref-range` |
| `inode.absent_nonzero` | MUST | Reject if present.nsec is clear and any *_nsec is nonzero, or present.crtime is clear and crtime_sec or crtime_nsec is nonzero. | `inode-nsec-absent-nonzero`, `inode-crtime-absent-nonzero` |
| `inode.capability_absent_nonzero` | MUST | Reject if capabilities.xattrs is clear and any inode has xattr_count > 0, or capabilities.fs_flags is clear and any fs_flags != 0, or capabilities.crtime is clear and any present.crtime is set. | `inode-xattrs-without-capability` |
| `inode.extent_tiling` | MUST | Reject unless inode ranges [extent_first, extent_first + extent_count) tile the extent table in inode order: the first is 0, each begins where the previous ended, the last ends at extent_count. | `inode-extent-tiling` |
| `inode.xattr_tiling` | MUST | Reject unless inode ranges [xattr_first, xattr_first + xattr_count) tile the xattr table in inode order. | `inode-xattr-tiling` |
| `extent.order` | MUST | Reject unless each inode's extents are strictly ascending and non-overlapping: file_offset[i] + raw_len[i] <= file_offset[i+1]. | `extent-overlap` |
| `extent.beyond_size` | MUST | Reject an extent with file_offset + raw_len > the owning inode's size, computed without overflow. | `extent-beyond-size` |
| `xattr.name` | MUST | Reject an xattr whose name is empty, contains NUL, or exceeds XATTR_NAME_MAX, or whose value exceeds XATTR_VALUE_MAX. | `xattr-name-empty` |
| `xattr.order` | MUST | Reject unless each inode's xattrs are strictly ascending by name bytes (so unique). | `xattr-order` |
| `dir.bijection` | MUST | Reject unless dir_records and dir inodes correspond one to one, in the same ino order. | `dir-missing-record`, `dir-record-for-reg` |
| `dir.dirent_tiling` | MUST | Reject unless dir ranges [dirent_first, dirent_first + dirent_count) tile the dirent table in dir order. | `dir-dirent-tiling` |
| `dirent.name` | MUST | Reject a dirent name that is empty, longer than NAME_MAX, contains '/' or NUL, or is '.' or '..'. | `dirent-name-slash`, `dirent-name-dotdot` |
| `dirent.order` | MUST | Reject unless each directory's dirents are strictly ascending by name bytes (so unique). | `dirent-order` |
| `dirent.child_missing` | MUST | Reject a dirent whose child_ino is not in the inode table. | `dirent-child-missing` |
| `namespace.root` | MUST | Reject unless root_ino is in the inode table, is a dir, and is named by no dirent. | `namespace-root-not-dir`, `namespace-root-named` |
| `namespace.dir_links` | MUST | Reject unless every directory other than the root is named by exactly one dirent. | `namespace-dir-hardlink` |
| `namespace.orphan` | MUST | Reject a non-directory inode named by no dirent. | `namespace-orphan` |
| `namespace.unreachable` | MUST | Reject unless every directory is reachable from the root. | `namespace-cycle` |
| `index.inherited_in_full` | MUST | Reject an inherited inode in a full capture (base_ref == 0). | `index-inherited-in-full` |

## Phase `blocks`

Archives only (not sidecars): block tiling, unknown types, DATA agreement with extents, digests, decoding, file content digests.

| id | level | rule | fixtures |
|---|---|---|---|
| `blocks.tiling` | MUST | Reject unless blocks, each BLOCK_HEADER_LEN + payload_len long, tile [HEADER_LEN, index_offset) exactly. | `blocks-tiling` |
| `blocks.stray_index` | MUST | Reject an INDX block anywhere other than index_offset. | `blocks-stray-index` |
| `blocks.unknown_type` | MUST | Reject a block of unknown or registered-undefined type_id without flags.skip. Such a block with flags.skip is ignored. | `blocks-unknown-no-skip`, `blocks-registered-no-skip` |
| `extent.block_offset` | MUST | Reject an extent whose block_offset is not the start of a DATA block in the tiling. | `extent-block-offset` |
| `blocks.data_references` | MUST | Reject unless every DATA block is referenced by exactly one extent. | `blocks-data-unreferenced`, `blocks-data-shared` |
| `data.payload_digest` | MUST | Reject a block whose payload_digest != BLAKE3(payload). | `data-payload-digest` |
| `data.too_short` | MUST | Reject a DATA payload shorter than DATA_HEADER_LEN. | `data-too-short` |
| `data.codec` | MUST | Reject a DATA block whose codec is not a defined non-invalid value. | `data-codec` |
| `data.matches_extent` | MUST | Reject unless data_header.ino == owning inode's ino, file_offset == extent.file_offset, raw_len == extent.raw_len, raw_digest == extent.raw_digest. | `data-ino`, `data-file-offset`, `data-raw-len`, `data-raw-digest-field` |
| `data.stored_len` | MUST | Reject a stored-codec DATA block whose stored byte count != raw_len. | `data-stored-len` |
| `data.decode` | MUST | Reject a zstd DATA block whose stored bytes are not exactly one valid zstd frame decoding to exactly raw_len bytes. | `data-zstd-bad-frame`, `data-zstd-short` |
| `data.raw_digest` | MUST | Reject a DATA block where BLAKE3(decoded raw bytes) != raw_digest. | `data-raw-digest` |
| `inode.content_digest` | MUST | Reject an extents inode whose content_digest != BLAKE3 of its logical content: size bytes, extent bytes at their offsets, zeros elsewhere. | `inode-content-digest` |

## Phase `chain`

Only when ancestors are supplied: agreement of inherited inodes with the ancestor that stores them.

| id | level | rule | fixtures |
|---|---|---|---|
| `chain.ancestor_digest` | MUST | Reject if a supplied archive with a chain entry's archive_uuid has trailer.index_digest != that entry's index_digest. | `chain-ancestor-digest` |
| `chain.inherited_match` | MUST | Reject an inherited inode unless the referenced ancestor has an inode with the same ino, generation, size and content_digest, whose content_kind is extents. | `chain-inherited-digest`, `chain-inherited-not-stored` |

## Writer and behaviour obligations

| id | level | scope | rule | why no fixture |
|---|---|---|---|---|
| `writer.registered_types` | MUST | writer | A writer MUST NOT write a registered-undefined block type or enum value. | A reader treats these exactly as unknown values, which rules blocks.unknown_type and dir.completeness already cover. |
| `writer.heap_minimal` | SHOULD | writer | A writer SHOULD NOT emit heap bytes no reference addresses. | Unreferenced heap bytes are valid; a reader cannot reject them. |
| `policy.unverified_content` | MUST | behaviour | No tool emits bytes of a DATA block that failed data.payload_digest, data.decode or data.raw_digest. FUSE read returns EIO; restore does not create the file, continues with other files, and exits non-zero naming each failure. | Tool behaviour over a file the corpus already contains (data-raw-digest); tested by diavolo-fuse and diavolo-cli tests. |
| `policy.restore_target` | MUST | behaviour | A full restore MUST refuse a target directory containing any entry other than an empty lost+found, unless explicitly told to reconcile; reconciling removes every entry the index does not list. Nothing pre-existing survives silently (docs/design/0003, dump failure mode 3). | Tool behaviour against a target directory, not a property of any archive; tested by diavolo-cli restore tests. |
| `policy.sidecar_content` | MUST | behaviour | A content read through a sidecar fails with a distinct 'content not present' error, never a digest error. | Tool behaviour over golden sidecars; tested by diavolo-format and diavolo-fuse tests. |
